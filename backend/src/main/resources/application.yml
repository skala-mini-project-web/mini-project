spring:
  application:
    name: crosschecklab

  datasource:
    url: jdbc:postgresql://localhost:5432/crosschecklab
    username: ${DB_USERNAME:crosschecklab}
    password: ${DB_PASSWORD:crosschecklab}
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: validate   # 스키마는 Flyway가 관리, JPA는 검증만
    properties:
      hibernate:
        format_sql: true
    open-in-view: false

  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

logging:
  level:
    org.hibernate.SQL: debug